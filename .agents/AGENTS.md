# 🎯 Workspace Rules & Token Optimization Directives

## 👤 User Profile & Target Alignment
* **Name:** M. Haresh Kumar
* **Core Profile:** Data Operations Lead & Senior Analytics Specialist (14+ Years Data Operations Leadership)
* **Technical Stack:** 
  * Advanced SQL (CTEs, Window Functions, Star/Snowflake Schema Modeling, Query Optimization)
  * Power BI (DAX, Power Query ETL, Semantic Modeling, RLS/OLS)
  * Snowflake (Medallion Architecture: Bronze/Silver/Gold, Streams, Tasks, Dynamic PII Masking)
  * Python Automation (`pandas`, REST APIs, `gspread`, `pywin32`) & AI Workflows (Claude, Agentic AI, MCP)
* **Notice Period:** Immediate Joiner (0 Days / 15 Days Notice)
* **Target Locations:** Bengaluru / Hybrid / Remote

---

## ⚡ Context & Token Efficiency Rules

1. **Precision Skill Triggering:**
   - Do NOT load heavy skill documentation unless the user explicitly requests tasks matching that skill's exact domain.
   - Keep skill descriptions tightly scoped to avoid false triggering.

2. **Line-Bounded File Reading:**
   - Always view files using narrow `StartLine` and `EndLine` parameters instead of loading full 500+ line documents into memory.

3. **Concise Tool & Terminal Outputs:**
   - Keep command outputs concise (e.g., `git log -n 3`, targeted python scripts) to avoid filling the conversation context with unnecessary log output.

4. **Zero Re-Exploration:**
   - Do not re-inspect repository structure or configuration files if their contents were established earlier in the session.

5. **Aesthetics & Code Quality:**
   - When editing web or portfolio components, maintain modern dark-mode aesthetic standards, clean CSS styling, and responsive layout guidelines.
